apiVersion: v1
kind: PersistentVolumeClaim
metadata:
   name: food-app-pvc
   namespace: food-app
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: normal
  resources:
      requests: 
        storage: 2Gi
